[Unit]
Description=Vivobook RGB default color (boot + resume)
After=multi-user.target suspend.target

[Service]
Type=oneshot
EnvironmentFile=-/etc/vrgb.conf
ExecStart=/usr/bin/vrgb color ${VRGB_COLOR} ${VRGB_BRIGHTNESS}
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target suspend.target
